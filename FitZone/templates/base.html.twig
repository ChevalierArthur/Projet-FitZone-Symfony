<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="assets/css/bootstrap.css"/>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel='stylesheet' href='assets\css\style.rtl.css'>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
        <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">LogoFitzone</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_accueil')}}">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_presentation')}}">Presentation</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Prestations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_avis')}}">Avis</a>
        </li>
        {% if app.user and is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_messagerie')}}">Contact</a>
        </li>
        {% endif %}
        {% if app.user and is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_messagerie_message')}}">Message</a>
        </li>
        {% endif %}
      </ul>
      {% if not app.user %}
      <form class="d-flex" action="{{path('app_login')}}">
        <button type="submit" class="btn btn-connexion my-2 my-sm-0">Connexion</button>
      </form>
      {% endif %}
      {% if app.user %}
      <form class="d-flex" action="{{path('app_logout')}}">
        <button type="submit" class="btn btn-connexion my-2 my-sm-0">Déconnexion</button>
      </form>
      {% endif %}
    </div>
  </div>
</nav>
    </head>
    <body>
        {% block body %}
        {% endblock %}
    </body>
    <footer>
    </footer>
</html>
